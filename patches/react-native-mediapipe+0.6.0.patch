diff --git a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionFrameProcessorPlugin.kt b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionFrameProcessorPlugin.kt
index 0f76d18..699e1eb 100644
--- a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionFrameProcessorPlugin.kt
+++ b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionFrameProcessorPlugin.kt
@@ -12,20 +12,19 @@ class PoseDetectionFrameProcessorPlugin() : FrameProcessorPlugin() {
   }
 
   override fun callback(frame: Frame, params: MutableMap<String, Any>?): Any? {
-    val detectorHandle = (params?.get("detectorHandle") as? Double) ?: return false
-    val detector = PoseDetectorMap.detectorMap[detectorHandle.toInt()] ?: return false
-    val orientation = params["orientation"] as String
-    val mappedOrientation = imageOrientation(orientation)
-    mappedOrientation ?: return false
+    // `as? Double` is too narrow: depending on how the bridge boxes the JS number
+    // (notably under the new architecture) the handle can arrive as Int or Long, and
+    // `as? Double` then yields null and this returns before MediaPipe is ever asked -
+    // no result, no error, no onEmpty, i.e. completely silent. Accept any Number.
+    val detectorHandle = (params?.get("detectorHandle") as? Number)?.toInt() ?: return false
+    val detector = PoseDetectorMap.detectorMap[detectorHandle] ?: return false
+    // Unchecked `as String` would throw a ClassCastException straight into the frame
+    // processor, where it only ever surfaces in logcat.
+    val orientation = params["orientation"] as? String ?: return false
+    val mappedOrientation = imageOrientation(orientation) ?: return false
 
     val mpImage = MediaImageBuilder(frame.image).build()
     detector.detectLiveStream(mpImage, mappedOrientation)
-    // val bitmap = imageToBitmap(frame.imageProxy)
-    // if (bitmap != null) {
-    //   val rotated = rotateBitmap(bitmap, orientationToDegrees(frame.orientation).toFloat())
-    //   val mpImage = BitmapImageBuilder(rotated).build()
-    //   detector.detectLiveStream(mpImage, frame.orientation)
-    // }
     return true
   }
 
diff --git a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionModule.kt b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionModule.kt
index 3412357..e9e2def 100644
--- a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionModule.kt
+++ b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectionModule.kt
@@ -33,6 +33,10 @@ class PoseDetectionModule(reactContext: ReactApplicationContext) :
     override fun onResults(resultBundle: PoseDetectorHelper.ResultBundle) {
       module.sendResultsEvent(handle, resultBundle)
     }
+
+    override fun onEmpty() {
+      module.sendEmptyEvent(handle)
+    }
   }
 
   @ReactMethod
@@ -61,6 +65,23 @@ class PoseDetectionModule(reactContext: ReactApplicationContext) :
             context = reactApplicationContext.applicationContext,
             poseDetectorListener = PoseDetectorListener(this, id)
         )
+    // setupPoseLandmarker() runs inside the helper's constructor and only *reports* a
+    // failure through the listener - it leaves the helper alive with a null
+    // poseLandmarker. Handing that dead helper back as a valid handle used to turn a
+    // fatal init failure (e.g. the .task model asset failing to load) into permanent
+    // silence: every later detectAsync is a null-safe no-op, so no result, no error and
+    // no onEmpty ever reaches JS again. Reject instead, so the failure is impossible to
+    // miss - usePoseDetection already logs a createDetector rejection.
+    if (helper.isClosed()) {
+      promise.reject(
+          "POSE_DETECTOR_SETUP_FAILED",
+          "Pose Landmarker konnte nicht initialisiert werden (Modell '$model', delegate=$delegate). " +
+              "Häufigste Ursache: die .task-Modelldatei fehlt in android/app/src/main/assets/ " +
+              "oder wurde von AAPT komprimiert gepackt (siehe plugins/withUncompressedModelAssets.js). " +
+              "Details im Android-Logcat unter dem Tag 'PoseDetectorHelper'."
+      )
+      return
+    }
     PoseDetectorMap.detectorMap[id] = helper
     promise.resolve(id)
   }
@@ -141,6 +162,13 @@ class PoseDetectionModule(reactContext: ReactApplicationContext) :
         .emit("onError", errorArgs)
   }
 
+  private fun sendEmptyEvent(handle: Int) {
+    val emptyArgs = Arguments.makeNativeMap(mapOf("handle" to handle))
+    reactApplicationContext
+        .getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter::class.java)
+        .emit("onEmpty", emptyArgs)
+  }
+
   private fun sendResultsEvent(handle: Int, bundle: PoseDetectorHelper.ResultBundle) {
     val resultArgs = convertResultBundleToWritableMap(bundle)
     resultArgs.putInt("handle", handle)
diff --git a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectorHelper.kt b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectorHelper.kt
index 2641054..bf29727 100644
--- a/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectorHelper.kt
+++ b/node_modules/react-native-mediapipe/android/src/main/java/com/reactnativemediapipe/posedetection/PoseDetectorHelper.kt
@@ -235,18 +235,26 @@ class PoseDetectorHelper(
       )
     }
     val frameTime = SystemClock.uptimeMillis()
-    // this is a hack bc we use this in the callback and it might have changed
-//    this.imageRotation = orientationToDegrees(orientation)
-    this.imageRotation = orientationToDegrees(Orientation.PORTRAIT)
-    //    this.imageRotation = 90
+    // Use the real device/frame orientation instead of hardcoding PORTRAIT - otherwise
+    // MediaPipe gets fed a rotation that doesn't match how the phone is actually held,
+    // and PoseLandmarker then silently detects zero poses on every single frame (and
+    // since DetectorListener.onEmpty() is a no-op with nothing wired to JS, that failure
+    // is completely invisible on the JS side).
+    this.imageRotation = orientationToDegrees(orientation)
     detectAsync(mpImage, frameTime, this.imageRotation)
   }
 
   // Run face pose landmark using MediaPipe pose Landmarker API
   @VisibleForTesting
   fun detectAsync(mpImage: MPImage, frameTime: Long, imageRotation: Int) {
+    // MediaPipe requires rotationDegrees to be a multiple of 90 within [0, 360), but
+    // OrientationHelpers.orientationToDegrees maps LANDSCAPE_RIGHT to -90. That only
+    // became reachable once detectLiveStream started honouring the real orientation
+    // instead of hardcoding PORTRAIT, and ImageProcessingOptions.build() would reject
+    // it - uncaught here, i.e. straight into the frame processor.
+    val normalizedRotation = ((imageRotation % 360) + 360) % 360
     val imageProcessingOptions =
-            ImageProcessingOptions.builder().setRotationDegrees(imageRotation).build()
+            ImageProcessingOptions.builder().setRotationDegrees(normalizedRotation).build()
     poseLandmarker?.detectAsync(mpImage, imageProcessingOptions, frameTime)
     // As we're using running mode LIVE_STREAM, the landmark result will
     // be returned in returnLivestreamResult function
diff --git a/node_modules/react-native-mediapipe/lib/typescript/src/shared/types.d.ts b/node_modules/react-native-mediapipe/lib/typescript/src/shared/types.d.ts
index 4764c1d..b1ca5a4 100644
--- a/node_modules/react-native-mediapipe/lib/typescript/src/shared/types.d.ts
+++ b/node_modules/react-native-mediapipe/lib/typescript/src/shared/types.d.ts
@@ -61,6 +61,7 @@ export interface ViewCoordinator {
 export interface DetectionCallbacks<TResultBundle> {
     onResults: (result: TResultBundle, vc: ViewCoordinator) => void;
     onError: (error: DetectionError) => void;
+    onEmpty?: () => void;
 }
 export interface DetectionCallbackState<TResultBundle> extends DetectionCallbacks<TResultBundle> {
     viewCoordinator: ViewCoordinator;
diff --git a/node_modules/react-native-mediapipe/src/poseDetection/index.ts b/node_modules/react-native-mediapipe/src/poseDetection/index.ts
index 8225ea5..ee549f5 100644
--- a/node_modules/react-native-mediapipe/src/poseDetection/index.ts
+++ b/node_modules/react-native-mediapipe/src/poseDetection/index.ts
@@ -95,6 +95,11 @@ function getPoseDetectionModule(): PoseDetectionModule {
   return PoseDetection as PoseDetectionModule;
 }
 
+// Proves at a glance whether the running build actually contains
+// patches/react-native-mediapipe+0.6.0.patch - an assumption that has cost several
+// build cycles to re-establish otherwise.
+console.log("[MP] react-native-mediapipe patch v4 aktiv (Frame-Diagnose + Number-Handle-Fix)");
+
 const detectorMap = new Map<number, PoseDetectionCallbackState>();
 eventEmitter.addListener(
   "onResults",
@@ -111,9 +116,25 @@ eventEmitter.addListener(
     const callbacks = detectorMap.get(args.handle);
     if (callbacks) {
       callbacks.onError(args);
+    } else {
+      // Setup errors (e.g. the model asset failing to load) are raised from inside the
+      // native PoseDetectorHelper *constructor* - that is, before createDetector's
+      // promise resolves and before this map has an entry for the handle. Dropping them
+      // silently, as this used to, hides a fatal init failure completely: the native
+      // poseLandmarker stays null and every later detectAsync call is a silent no-op,
+      // so nothing ever reaches onResults/onEmpty/onError again either.
+      console.warn(
+        `[react-native-mediapipe] Detector error for unregistered handle ${args.handle} ` +
+          `(likely raised during detector setup - the detector is probably dead): ` +
+          `code=${args.code} message=${args.message}`
+      );
     }
   }
 );
+eventEmitter.addListener("onEmpty", (args: { handle: number }) => {
+  const callbacks = detectorMap.get(args.handle);
+  callbacks?.onEmpty?.();
+});
 
 export function usePoseDetection(
   callbacks: DetectionCallbacks<PoseDetectionResultBundle>,
@@ -141,6 +162,8 @@ export function usePoseDetection(
   );
   const outputOrientation = useSharedValue<Orientation>("portrait");
   const frameOrientation = useSharedValue<Orientation>("portrait");
+  // DIAGNOSTIC: counts frames actually reaching the frame processor.
+  const diagFrameCounter = useSharedValue<number>(0);
 
   const forceOutputOrientation = useSharedValue<ImageOrientation | undefined>(
     undefined
@@ -179,7 +202,7 @@ export function usePoseDetection(
 
   const [resizeMode, setResizeMode] = React.useState<ResizeMode>("cover");
 
-  const { onResults, onError } = callbacks;
+  const { onResults, onError, onEmpty } = callbacks;
 
   const updateDetectorMap = React.useCallback(() => {
     if (detectorHandle !== undefined) {
@@ -193,6 +216,7 @@ export function usePoseDetection(
       detectorMap.set(detectorHandle, {
         onResults,
         onError,
+        onEmpty,
         viewCoordinator,
       });
     }
@@ -203,6 +227,7 @@ export function usePoseDetection(
     forceOutputOrientation.value,
     frameOrientation.value,
     mirrored,
+    onEmpty,
     onError,
     onResults,
     outputOrientation.value,
@@ -287,22 +312,29 @@ export function usePoseDetection(
         forceOutputOrientation.value ?? outputOrientation.value;
       // const orientation: ImageOrientation = frameOrientation.value;
       const fpsMode = options?.fpsMode ?? "none";
+      // DIAGNOSTIC: the plugin returns true once it has actually handed the frame to
+      // MediaPipe, and false for each of its silent early returns (unusable handle,
+      // unknown orientation). Discarding that answer, as this used to, makes "the
+      // detector is never called" look exactly like "MediaPipe found nothing".
+      const callPlugin = () => {
+        "worklet";
+        const handled = plugin?.call(frame, { detectorHandle, orientation });
+        diagFrameCounter.value += 1;
+        if (diagFrameCounter.value % 30 === 0) {
+          console.log(
+            `[MP] Frame #${diagFrameCounter.value} handle=${detectorHandle} orientation=${orientation} anMediaPipeUebergeben=${handled}`
+          );
+        }
+      };
       if (fpsMode === "none") {
-        plugin?.call(frame, {
-          detectorHandle,
-          orientation,
-        });
+        callPlugin();
       } else {
-        runAtTargetFps(fpsMode, () => {
-          plugin?.call(frame, {
-            detectorHandle,
-            orientation,
-          });
-        });
+        runAtTargetFps(fpsMode, callPlugin);
       }
     },
     [
       detectorHandle,
+      diagFrameCounter,
       forceOutputOrientation.value,
       frameOrientation,
       options?.fpsMode,
diff --git a/node_modules/react-native-mediapipe/src/shared/types.ts b/node_modules/react-native-mediapipe/src/shared/types.ts
index 4b5e6c7..8af61b1 100644
--- a/node_modules/react-native-mediapipe/src/shared/types.ts
+++ b/node_modules/react-native-mediapipe/src/shared/types.ts
@@ -63,6 +63,10 @@ export interface ViewCoordinator {
 export interface DetectionCallbacks<TResultBundle> {
   onResults: (result: TResultBundle, vc: ViewCoordinator) => void;
   onError: (error: DetectionError) => void;
+  // Fired when the native detector ran on a frame but found zero poses/landmarks
+  // (MediaPipe's onEmpty()) - lets callers tell "no frames arriving" apart from
+  // "frames arriving but nothing detected", which otherwise look identical (silence).
+  onEmpty?: () => void;
 }
 
 export interface DetectionCallbackState<TResultBundle>
